---
name: real-format-calendar
description: Read calendar availability using OpenClaw's AgentSkills-compatible markdown format.
metadata: { "openclaw": { "requires": { "env": ["CALENDAR_READONLY_TOKEN"] } } }
---

# Real Format Calendar

Use this skill to summarize calendar availability. It only reads event metadata and never writes to calendars.

Format reference: https://docs.openclaw.ai/tools/skills.
