{
  "name": "@runbrake/contracts",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "tsc -p tsconfig.json --noEmit",
    "schema:check": "pnpm run test",
    "test": "tsc -p tsconfig.json && node --test dist/**/*.test.js"
  }
}
